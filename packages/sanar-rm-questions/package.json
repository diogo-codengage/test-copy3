{
  "name": "sanar-rm-questions",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@types/react-dom": "^16.8.4",
    "typescript": "^3.5.1"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build && mv build questoes && mkdir build && mv -v questoes build/",
    "test": "react-app-rewired test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
